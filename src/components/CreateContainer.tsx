import * as React from 'react';

export interface ICreateContainerProps {
}

export function CreateContainer (props: ICreateContainerProps) {
  return (
    <div>
      CreateContainer
    </div>
  );
}

export default CreateContainer;
